How to use :
1. Clone the repository
2. Install the requirements.txt file (pip install -r path_to_requirements.txt)
3. Open a terminal and type : python -m path_to_python_file.py rosetta_email rosetta_password
4. Leave it in the background and wait

Note : You must have chrome installed on your computer first (AND python (Yes you need that too, crazy right ?))
